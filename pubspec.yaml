name: mini_dashboard_widgets
description: Beautiful, highly customizable dashboard widgets including stat cards and charts
version: 0.1.0
homepage: https://github.com/yourusername/mini_dashboard_widgets

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
